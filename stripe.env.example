# Copy this file to ".env" (do not commit it) and replace placeholders with real values.

# Flask
FLASK_ENV=development
FLASK_DEBUG=1
APP_HOST=127.0.0.1
APP_PORT=5000

#
# Admin UI (Basic Auth)
# - Used to protect: GET /config, PUT /api/config, PUT /api/catalog
# - Username defaults to "admin" if ADMIN_USERNAME is not set.
ADMIN_PASSWORD=change_me
# Optional:
# ADMIN_USERNAME=admin

# Optional (Address Element autocomplete when used standalone):
# If you want built-in address autocomplete, provide your own Google Maps "Places API" key.
# This is a browser key; restrict it by HTTP referrer in Google Cloud Console.
GOOGLE_MAPS_PLACES_API_KEY=AIzaSy_PLACEHOLDER

# Stripe configuration is stored in JSON files (live-editable from /config):
# - config/runtime-config.json (accounts, keys, webhook signing secrets, CPM mapping, master alias)
# - config/catalog.json (product + prices)



